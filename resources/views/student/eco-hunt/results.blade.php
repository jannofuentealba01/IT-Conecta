@extends('layouts.app')
@section('content')
<style>.results{max-width:720px;margin:auto}.hero{background:linear-gradient(135deg,var(--brand-green-dark),var(--brand-blue));color:var(--surface);padding:23px;border-radius:20px;text-align:center}.row{display:grid;grid-template-columns:50px 1fr auto;gap:10px;align-items:center;background:var(--surface);border:1px solid var(--border);border-radius:13px;padding:14px;margin-top:9px}.mine{border:2px solid var(--brand-green);background:var(--positive-soft)}.place{font-size:21px;font-weight:900;color:var(--brand-green-dark)}.points{text-align:right;font-weight:900;color:var(--warning-orange)}.back{display:flex;justify-content:center;background:var(--brand-green);color:var(--surface);text-decoration:none;padding:14px;border-radius:12px;margin-top:16px;font-weight:800}.back:hover{background:var(--brand-green-dark)}</style>
<main class="results"><div class="hero"><h1>🏆 Resultados de EcoBúsqueda</h1><p>El mayor puntaje gana. En caso de empate se usa el orden técnico de registro, sin mostrarlo durante la partida.</p></div>@forelse($ranking as $index=>$entry)<div class="row {{ $entry->id===$participant->id?'mine':'' }}"><div class="place">{{ $index===0?'🏆':'#'.($index+1) }}</div><div><strong>{{ $entry->name }}</strong>@if($entry->id===$participant->id) · Tú @endif<br><small>{{ $entry->completed_count }} actividades encontradas</small></div><div class="points">{{ $entry->points }} pts</div></div>@empty<p>No se registraron participantes.</p>@endforelse<div class="row" style="display:block;margin-top:18px"><strong>¿Quieres seguir ayudando?</strong><p>🌳 Planta y cuida un árbol con apoyo de tu comunidad.</p><p>📣 Propón una campaña ambiental en tu curso.</p></div><a class="back" href="{{ route('student.dashboard') }}">Volver al panel</a></main>
@endsection
